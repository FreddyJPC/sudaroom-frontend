// src/App.tsx
import RouterComponent from './routes'; // Importamos el componente Router

const App = () => {
  return (
    <RouterComponent /> // Solo renderizamos las rutas aquí
  );
};

export default App;
